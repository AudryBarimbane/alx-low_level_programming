#include "main.h"
#include <stdio.h>

/**
 * main - name of the program
 * @argc: number
 * @argv: array
 *
 * Return: int
 */
int main(int  __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
